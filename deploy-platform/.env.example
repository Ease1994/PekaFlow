# docker compose 同级。复制为 .env 即可启动，不必先改。
# 不要把填了值的 .env 提交进 Git。
#
# 下面是试用默认值。生产或对外服务请改口令后 `docker compose up -d`。
# 已经在跑的卷改 MYSQL / Redis 口令不会生效（容器里还是建库时那把），
# 要换口令就 `docker compose down -v` 后重来（库和日志会清空）。
#
# JWT_SECRET / AES_KEY / HARNESS_RUNNER_TOKEN 空着：首次启动写入 data/.secrets.json，
# 重启仍用同一把。已经在跑的环境不要重新随机，否则库里 Git 凭证解不开。
JWT_SECRET=
AES_KEY=
HARNESS_RUNNER_TOKEN=
#
# 两处 MySQL 口令必须相同。主机名在 compose 网络里是 mysql。
MYSQL_ROOT_PASSWORD=rp-mysql
DATABASE_URL=mysql+pymysql://root:rp-mysql@mysql:3306/deploy_platform?charset=utf8mb4
#
REDIS_PASSWORD=rp-redis
#
# Compose 自带 ES。设置页可改成自备集群。
ES_HOSTS=http://elasticsearch:9200
#
# 管理员：admin / admin123。登录后请改密。
BOOTSTRAP_ADMIN_PASSWORD=admin123
#
# 浏览器跨域白名单，逗号分隔。Compose 下前端同源反代 /api，一般不用填。
CORS_ORIGINS=
