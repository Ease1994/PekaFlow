---
description: 生产安全优先、宁可报错也不能把风险放上环境
alwaysApply: true
---

# 生产安全优先、宁可报错也不能把风险放上环境

这是设计和写代码的第一原则。流水线报错、页面卡住都不值一提；把未点名、未确认的变更放上环境才是事故。

## 失败关闭

- 范围不明就拒绝执行，不要猜最大范围。空清单、空命名空间、空节点、空镜像、空目标目录，都不能当成「全部 / default / latest」。
- 禁止用静默兜底把 blast radius 放大：空输入不要翻译成 `**`、全量打包、全部节点、跳过审批。
- `except` 之后若仍可能继续部署，必须先停。YAML 解析失败、变量没替换、白名单缺失，执行路径一律失败，不能降级成「还能发」。
- 测试环境为了方便的默认，禁止当成生产的默认。缺配置按生产收紧（关），不按测试放行（开）。

## 明确写出才算全量

要整包发、打全部文件、发到 `default` 命名空间，必须在步骤或当次执行里写明。空着、没填、上次误注入的 `**`，都不能再带上环境。

## 反例（禁止再犯）

增量包步骤仍是 `${{DEPLOY_MANIFEST}}`，执行弹窗没填清单 → 禁止写入 `**` 全量打包。正确做法：拒绝执行，提示填写清单或把文件列表写死在步骤里。
